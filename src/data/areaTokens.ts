/** Tokens partilhados mapa ↔ course engine — evita ciclo imports `courses` ↔ `@/content/courses`. */
export type AreaColor = "canna" | "purple" | "amber" | "cyan" | "rose";

export type AreaLevelLabel = "Iniciante" | "Intermediário" | "Avançado" | "Todos os níveis";
